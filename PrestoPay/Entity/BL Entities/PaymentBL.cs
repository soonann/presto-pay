using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PrestoPay.Entity.BL_Entities
{
    public class PaymentBL
    {
        /*
        public bool MakePayment(double amount, string description, string type, string from ,string to)
        {
            
            bool add = new AccountDAO().AddAmountToWallet(to,amount);
            bool deduct = new AccountDAO().DeductAmountFromWallet(from,amount);
            string insert = new TransactionDAO().InsertTransaction( amount, description,  type,  from,  to);

            return (add && deduct && String.IsNullOrEmpty(insert));

        }
        */
    }
}